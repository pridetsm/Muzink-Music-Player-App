<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <Button
        android:id="@+id/add_edit_cancel_button"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/ic_close_black_24dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>
    <EditText
        android:id="@+id/playlist_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="My playlist"
        android:drawableRight="@drawable/ic_playlist_icon"
        android:textColorHint="#33888888"
        app:layout_constraintTop_toBottomOf="@id/add_edit_cancel_button"
        app:layout_constraintVertical_bias="0.3"
        app:layout_constraintBottom_toBottomOf="parent"
         />
    <Button
        android:id="@+id/save_play_list_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="save"
        android:textAllCaps="false"
       app:layout_constraintTop_toBottomOf="@id/playlist_edit_text"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintVertical_bias="0.1"
        android:textColor="#ffffff"/>

</androidx.constraintlayout.widget.ConstraintLayout>